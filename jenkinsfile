pipeline {
    agent any
        stages {
        stage('Checkout SCM') {
            steps {
                checkout scm
            }
        }
        stage('Check SF version'){
        steps{
            script{
                sh 'sf version'
            }
        }
        }
}